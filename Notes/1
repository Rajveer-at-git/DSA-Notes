/*
# To find the sum of the values in an array 

vector<int> nums = {5, 10, 15, 20};

    // Calculate the sum starting with an initial value of 0
    int sum = accumulate(nums.begin(), nums.end(), 0);

# To find the no. of elements in the array
    
    n = nums.size();

# 
    */